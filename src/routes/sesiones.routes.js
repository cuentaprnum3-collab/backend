const r=require("express").Router();const c=require("../controllers/sesiones.controller");const{verificarToken}=require("../middlewares/auth.middleware");r.use(verificarToken);r.get("/",c.listar);r.post("/",c.crear);r.put("/:id",c.actualizar);r.delete("/:id",c.eliminar);module.exports=r;
